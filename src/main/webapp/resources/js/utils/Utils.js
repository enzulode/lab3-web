// applyes an error message to error block
function applyErrorMessage(message) {
    $('#error-message').text(message);
}

// clears error block
function clearErrors() {
    $('#error-message').text('');
}